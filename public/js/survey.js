// Survey questions and form are now rendered server-side by EJS.
// The form POSTs to /api/friendsArray and the server redirects to /match.
// No client-side JS is needed for the survey form submission.